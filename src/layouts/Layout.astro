---
interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  schema?: string;
}

const { title, description, canonicalUrl, schema } = Astro.props;
const currentUrl = canonicalUrl || Astro.url.href;
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-PN5M8SS4RE"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-PN5M8SS4RE');
    </script>
    
    <!-- SEO Básico -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={currentUrl} />
    <meta name="robots" content="index, follow" />
    
    <!-- Open Graph (Facebook / WhatsApp) -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={currentUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://enioautosocorro.com.br/images/banner.png" />
    <meta property="og:locale" content="pt_BR" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={currentUrl} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content="https://enioautosocorro.com.br/images/banner.png" />

    <!-- Icons -->
    <link rel="icon" type="image/png" href="/images/logo.png" />
    <meta name="generator" content={Astro.generator} />

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Schema.org Structured Data -->
    {schema && <script type="application/ld+json" set:html={schema} />}
    
    <!-- Local Business Schema por Padrão -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "AutoTowing",
        "name": "Enio Auto Socorro e Guincho Goiânia",
        "image": "https://enioautosocorro.com.br/images/logo.png",
        "@id": "https://enioautosocorro.com.br/#localbusiness",
        "url": "https://enioautosocorro.com.br",
        "telephone": "+5562991934930",
        "priceRange": "$$",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Rua Recife, Qd. 02, lt. 22",
          "addressLocality": "Setor Urias Magalhães",
          "addressRegion": "Goiânia - GO",
          "postalCode": "74565-170",
          "addressCountry": "BR"
        },
        "geo": {
          "@type": "GeoCoordinates",
          "latitude": -16.6385,
          "longitude": -49.2789
        },
        "openingHoursSpecification": {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": [
            "Monday",
            "Tuesday",
            "Wednesday",
            "Thursday",
            "Friday",
            "Saturday",
            "Sunday"
          ],
          "opens": "00:00",
          "closes": "23:59"
        },
        "areaServed": [
          {
            "@type": "AdministrativeArea",
            "name": "Goiânia"
          },
          {
            "@type": "AdministrativeArea",
            "name": "Aparecida de Goiânia"
          }
        ]
      }
    </script>
  </head>
  <body>
    <!-- Top Bar Emergency -->
    <div class="top-bar">
      <div class="container top-bar-content">
        <span class="emergency-badge"><span class="pulse"></span> ATENDIMENTO DE EMERGÊNCIA 24 HORAS</span>
        <div class="top-bar-contacts">
          <a href="tel:+5562991934930" class="top-contact-link">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
            (62) 99193-4930
          </a>
          <a href="https://api.whatsapp.com/send?phone=5562991934930" target="_blank" rel="noopener noreferrer" class="top-contact-link top-whatsapp">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path></svg>
            WhatsApp
          </a>
        </div>
      </div>
    </div>

    <!-- Header Navigation -->
    <header class="main-header">
      <div class="container header-container">
        <a href="/" class="logo-link">
          <img src="/images/logo.png" alt="Enio Auto Socorro Logo" class="header-logo" width="160" height="42" />
        </a>
        
        <button class="mobile-menu-toggle" id="mobile-toggle" aria-label="Abrir Menu">
          <span class="bar"></span>
          <span class="bar"></span>
          <span class="bar"></span>
        </button>

        <nav class="nav-menu" id="nav-menu">
          <a href="/#servicos" class="nav-link">Serviços</a>
          <a href="/#bairros" class="nav-link">Bairros Atendidos</a>
          <a href="/#sobre" class="nav-link">Quem Somos</a>
          <a href="/#faq" class="nav-link">Dúvidas Comuns</a>
          <div class="nav-cta-group">
            <a href="tel:+5562991934930" class="btn-tel-call">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
              LIGAR AGORA
            </a>
            <a href="https://api.whatsapp.com/send?phone=5562991934930" target="_blank" rel="noopener noreferrer" class="btn-wa-call">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path></svg>
              WHATSAPP
            </a>
          </div>
        </nav>
      </div>
    </header>

    <!-- Main Content Area -->
    <main>
      <slot />
    </main>

    <!-- Footer Section -->
    <footer class="main-footer">
      <div class="container footer-grid">
        <div class="footer-col-about">
          <img src="/images/logo.png" alt="Enio Auto Socorro Logo" class="footer-logo" />
          <p class="footer-about-text">
            O <strong>Enio Auto Socorro</strong> é sinônimo de rapidez, confiança e atendimento humanizado. Com frota própria de guinchos plataforma modernos, estamos prontos para remover seu carro, moto ou caminhonete leve 24 horas por dia em qualquer ponto de Goiânia e Aparecida de Goiânia.
          </p>
          <div class="footer-emergency">
            <span class="emerg-txt">Emergência 24H:</span>
            <a href="tel:+5562991934930" class="emerg-phone">(62) 99193-4930</a>
          </div>
        </div>
        
        <div class="footer-col-links">
          <h3>Serviços Reboque</h3>
          <ul>
            <li><a href="/#servicos">Guincho para Carros</a></li>
            <li><a href="/#servicos">Guincho para Motos</a></li>
            <li><a href="/#servicos">Reboque de Caminhonetes</a></li>
            <li><a href="/#servicos">Auxílio Pane Elétrica/Mecânica</a></li>
            <li><a href="/#servicos">Recarga de Bateria (Chupeta)</a></li>
          </ul>
        </div>
        
        <div class="footer-col-contact">
          <h3>Nossa Localização</h3>
          <p class="footer-contact-item">
            <strong>Base Operacional:</strong><br />
            Rua Recife, Qd. 02, lt. 22<br />
            Setor Urias Magalhães<br />
            Goiânia - GO
          </p>
          <p class="footer-contact-item">
            <strong>Contatos Rápidos:</strong><br />
            WhatsApp/Ligações: <a href="tel:+5562991934930">(62) 99193-4930</a><br />
            Telefone Auxiliar: <a href="tel:+5562984300346">(62) 98430-0346</a>
          </p>
        </div>
      </div>
      
      <!-- Footer Bottom -->
      <div class="footer-bottom">
        <div class="container footer-bottom-content">
          <p>&copy; {new Date().getFullYear()} Enio Auto Socorro. Todos os direitos reservados. Goiânia - GO.</p>
          <div class="footer-seo-links">
            <a href="/robots.txt">robots.txt</a> | <a href="/sitemap.xml">sitemap.xml</a>
          </div>
        </div>
      </div>
    </footer>

    <!-- Persistent Floating WhatsApp Button -->
    <a 
      href="https://api.whatsapp.com/send?phone=5562991934930" 
      target="_blank" 
      rel="noopener noreferrer" 
      class="whatsapp-floating-button"
      aria-label="Falar no WhatsApp com Enio Auto Socorro"
    >
      <svg class="wa-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path>
      </svg>
      <span class="wa-tooltip">Chamar Guincho 24h</span>
    </a>

    <!-- Hamburger menu control -->
    <script>
      const mobileToggle = document.getElementById('mobile-toggle');
      const navMenu = document.getElementById('nav-menu');
      
      if (mobileToggle && navMenu) {
        mobileToggle.addEventListener('click', () => {
          mobileToggle.classList.toggle('active');
          navMenu.classList.toggle('active');
        });

        // Fechar menu ao clicar em links
        const navLinks = navMenu.querySelectorAll('a');
        navLinks.forEach(link => {
          link.addEventListener('click', () => {
            mobileToggle.classList.remove('active');
            navMenu.classList.remove('active');
          });
        });
      }
    </script>
  </body>
</html>

<style is:global>
  /* Global Design Tokens & Variables */
  :root {
    --primary: #003da5; /* O azul escuro premium da Enio Auto Socorro */
    --primary-dark: #002263;
    --primary-light: #eff6ff;
    --accent: #22c55e; /* Verde WhatsApp */
    --accent-hover: #16a34a;
    --danger: #dc2626; /* Vermelho Emergência */
    --dark: #0f172a; /* Slate 900 */
    --gray-dark: #334155; /* Slate 700 */
    --gray-light: #f1f5f9; /* Slate 100 */
    --white: #ffffff;
    
    --font-heading: 'Outfit', sans-serif;
    --font-body: 'Plus Jakarta Sans', sans-serif;
    
    --max-width: 1200px;
    --header-height: 80px;
    
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
    
    --radius-sm: 6px;
    --radius-md: 12px;
    --radius-lg: 20px;
  }

  /* Base Reset */
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    scroll-behavior: smooth;
    font-size: 16px;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    font-family: var(--font-body);
    color: var(--dark);
    background-color: var(--white);
    line-height: 1.6;
    overflow-x: hidden;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-heading);
    font-weight: 700;
    line-height: 1.2;
    color: var(--primary-dark);
  }

  a {
    color: inherit;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  .container {
    width: 100%;
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 20px;
  }

  section {
    padding: 80px 0;
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 50px;
  }

  .section-header h2 {
    font-size: 2.2rem;
    margin-bottom: 15px;
    text-transform: uppercase;
    letter-spacing: -0.5px;
  }

  .section-header p {
    font-size: 1.1rem;
    color: var(--gray-dark);
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-family: var(--font-heading);
    font-weight: 700;
    padding: 14px 28px;
    border-radius: var(--radius-md);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
    font-size: 1rem;
  }

  .btn-primary {
    background-color: var(--primary);
    color: var(--white);
    box-shadow: 0 4px 14px rgba(0, 61, 165, 0.3);
  }

  .btn-primary:hover {
    background-color: var(--primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 61, 165, 0.4);
  }

  .btn-wa {
    background-color: var(--accent);
    color: var(--white);
    box-shadow: 0 4px 14px rgba(34, 197, 94, 0.3);
  }

  .btn-wa:hover {
    background-color: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(34, 197, 94, 0.4);
  }

  .btn-tel {
    background-color: var(--danger);
    color: var(--white);
    box-shadow: 0 4px 14px rgba(220, 38, 38, 0.3);
  }

  .btn-tel:hover {
    background-color: #b91c1c;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(220, 38, 38, 0.4);
  }

  /* Header & Navigation Styles */
  .top-bar {
    background-color: var(--dark);
    color: var(--white);
    font-size: 0.85rem;
    padding: 8px 0;
    font-weight: 600;
  }

  .top-bar-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .emergency-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: #fca5a5;
  }

  .pulse {
    display: inline-block;
    width: 8px;
    height: 8px;
    background-color: #ef4444;
    border-radius: 50%;
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
    animation: pulse-ring 1.5s infinite;
  }

  @keyframes pulse-ring {
    0% {
      transform: scale(0.95);
      box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
    }
    70% {
      transform: scale(1);
      box-shadow: 0 0 0 6px rgba(239, 68, 68, 0);
    }
    100% {
      transform: scale(0.95);
      box-shadow: 0 0 0 0 rgba(239, 68, 68, 0);
    }
  }

  .top-bar-contacts {
    display: flex;
    gap: 15px;
  }

  .top-contact-link {
    display: flex;
    align-items: center;
    gap: 5px;
    opacity: 0.9;
  }

  .top-contact-link:hover {
    opacity: 1;
    color: var(--accent);
  }

  .top-whatsapp {
    color: var(--accent);
  }

  .main-header {
    background-color: var(--white);
    box-shadow: var(--shadow-sm);
    position: sticky;
    top: 0;
    z-index: 100;
    height: var(--header-height);
    display: flex;
    align-items: center;
  }

  .header-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
  }

  .header-logo {
    max-height: 48px;
    width: auto;
    object-fit: contain;
  }

  .nav-menu {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .nav-link {
    font-weight: 600;
    color: var(--gray-dark);
    font-size: 0.95rem;
  }

  .nav-link:hover {
    color: var(--primary);
  }

  .nav-cta-group {
    display: flex;
    gap: 10px;
    margin-left: 10px;
  }

  .btn-tel-call, .btn-wa-call {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.85rem;
    padding: 10px 16px;
    border-radius: var(--radius-sm);
    color: var(--white);
    letter-spacing: 0.5px;
  }

  .btn-tel-call {
    background-color: var(--danger);
  }

  .btn-tel-call:hover {
    background-color: #b91c1c;
    transform: translateY(-1px);
  }

  .btn-wa-call {
    background-color: var(--accent);
  }

  .btn-wa-call:hover {
    background-color: var(--accent-hover);
    transform: translateY(-1px);
  }

  /* Hamburger Menu */
  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 26px;
    height: 18px;
    background: transparent;
    border: none;
    cursor: pointer;
  }

  .mobile-menu-toggle .bar {
    width: 100%;
    height: 3px;
    background-color: var(--primary);
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  /* Footer Styles */
  .main-footer {
    background-color: #0b1329;
    color: #e2e8f0;
    padding: 60px 0 0;
    font-size: 0.95rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 40px;
    padding-bottom: 50px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-logo {
    max-height: 55px;
    margin-bottom: 20px;
  }

  .footer-about-text {
    color: #94a3b8;
    margin-bottom: 25px;
    font-size: 0.9rem;
    max-width: 480px;
  }

  .footer-emergency {
    background: rgba(255, 255, 255, 0.03);
    border-left: 4px solid var(--danger);
    padding: 12px 20px;
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
    display: inline-block;
  }

  .footer-emergency .emerg-txt {
    font-size: 0.8rem;
    color: #f87171;
    font-weight: 700;
    display: block;
    text-transform: uppercase;
  }

  .footer-emergency .emerg-phone {
    font-size: 1.6rem;
    font-family: var(--font-heading);
    font-weight: 800;
    color: var(--white);
  }

  .footer-col-links h3, .footer-col-contact h3 {
    color: var(--white);
    font-size: 1.2rem;
    margin-bottom: 20px;
    position: relative;
    padding-bottom: 8px;
  }

  .footer-col-links h3::after, .footer-col-contact h3::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 40px;
    height: 3px;
    background-color: var(--primary);
  }

  .footer-col-links ul {
    list-style: none;
  }

  .footer-col-links ul li {
    margin-bottom: 12px;
  }

  .footer-col-links ul li a {
    color: #94a3b8;
  }

  .footer-col-links ul li a:hover {
    color: var(--white);
    padding-left: 5px;
  }

  .footer-contact-item {
    margin-bottom: 20px;
    color: #94a3b8;
    font-size: 0.9rem;
  }

  .footer-contact-item strong {
    color: var(--white);
  }

  .footer-contact-item a:hover {
    color: var(--white);
  }

  .footer-bottom {
    background-color: #060a16;
    padding: 20px 0;
    font-size: 0.85rem;
    color: #64748b;
  }

  .footer-bottom-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 15px;
  }

  .footer-seo-links a {
    color: #64748b;
  }

  .footer-seo-links a:hover {
    color: #94a3b8;
  }

  /* Floating WhatsApp Button */
  .whatsapp-floating-button {
    position: fixed;
    bottom: 30px;
    right: 30px;
    background-color: var(--accent);
    color: var(--white);
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 24px rgba(34, 197, 94, 0.4);
    z-index: 99;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    animation: wa-bounce 2s infinite;
  }

  .whatsapp-floating-button:hover {
    transform: scale(1.1);
    box-shadow: 0 8px 30px rgba(34, 197, 94, 0.6);
  }

  .wa-icon {
    width: 32px;
    height: 32px;
  }

  .wa-tooltip {
    position: absolute;
    right: 75px;
    background-color: var(--dark);
    color: var(--white);
    padding: 8px 16px;
    border-radius: var(--radius-sm);
    font-size: 0.85rem;
    font-weight: 700;
    white-space: nowrap;
    pointer-events: none;
    opacity: 0;
    transform: translateX(10px);
    transition: all 0.3s ease;
    box-shadow: var(--shadow-md);
  }

  .whatsapp-floating-button:hover .wa-tooltip {
    opacity: 1;
    transform: translateX(0);
  }

  @keyframes wa-bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  /* Responsive Design Media Queries */
  @media (max-width: 992px) {
    .footer-grid {
      grid-template-columns: 1.5fr 1fr;
    }
    .footer-col-contact {
      grid-column: span 2;
    }
  }

  @media (max-width: 768px) {
    section {
      padding: 60px 0;
    }
    .section-header h2 {
      font-size: 1.8rem;
    }
    
    /* Navigation mobile */
    .mobile-menu-toggle {
      display: flex;
    }
    
    .nav-menu {
      position: absolute;
      top: var(--header-height);
      left: 0;
      right: 0;
      background-color: var(--white);
      flex-direction: column;
      padding: 30px 20px;
      gap: 20px;
      border-bottom: 2px solid var(--gray-light);
      box-shadow: var(--shadow-md);
      opacity: 0;
      transform: translateY(-10px);
      pointer-events: none;
      transition: all 0.3s ease;
    }

    .nav-menu.active {
      opacity: 1;
      transform: translateY(0);
      pointer-events: auto;
    }

    .nav-cta-group {
      flex-direction: column;
      width: 100%;
      margin-left: 0;
      margin-top: 10px;
      gap: 12px;
    }

    .btn-tel-call, .btn-wa-call {
      justify-content: center;
      padding: 12px;
      font-size: 0.95rem;
    }

    .mobile-menu-toggle.active .bar:nth-child(1) {
      transform: translateY(7px) rotate(45deg);
    }

    .mobile-menu-toggle.active .bar:nth-child(2) {
      opacity: 0;
    }

    .mobile-menu-toggle.active .bar:nth-child(3) {
      transform: translateY(-8px) rotate(-45deg);
    }

    .top-bar-content {
      flex-direction: column;
      gap: 8px;
      text-align: center;
    }

    .footer-grid {
      grid-template-columns: 1fr;
    }
    
    .footer-col-contact {
      grid-column: span 1;
    }

    .footer-bottom-content {
      flex-direction: column;
      text-align: center;
    }
    
    .whatsapp-floating-button {
      bottom: 20px;
      right: 20px;
      width: 54px;
      height: 54px;
    }
    
    .wa-tooltip {
      display: none; /* Hide tooltips on mobile */
    }
  }
</style>
